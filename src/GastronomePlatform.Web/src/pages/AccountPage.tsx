/** Личный кабинет (приоритет 1, бриф 6): профиль (7 раздельных PUT) + подписка (нужен слайс GET my). Заглушка каркаса. */
export function AccountPage() {
  return (
    <section className="mx-auto w-full max-w-[1200px] px-6 py-16">
      <h1 className="text-[34px] font-[540] leading-[1.2]">Личный кабинет</h1>
      <p className="mt-4 text-ink-secondary">Кабинет — в разработке.</p>
    </section>
  );
}
